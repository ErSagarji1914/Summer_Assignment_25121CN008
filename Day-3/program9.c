#include <stdio.h>
int main()
{
    int num, i;

    printf("Enter a number:");
    scanf("%d",&num);

    if (num / i == 1)
    {
        num == i;
    
    printf("The number is prime number");
    }
    else
    {
        printf("The number is  not a prime number");
    }
    return 0;
}